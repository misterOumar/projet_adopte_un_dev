<form method="post" action="{{ path('app_technologie_delete', {'id': technologie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ technologie.id) }}">
    <button class="btn">Delete</button>
</form>
